# 2026-07-31

## 配置白霜方案（自然码双拼）个人定制

用户环境要点：
- 本仓库 `D:\Documents\GitHub\rime-frost` 是 gaboolic/rime-frost 的新克隆，准备取代旧的 Rime 用户目录 `D:\Documents\Rime Config`（小狼毫 Weasel 0.17.4 / librime 1.13.1）。
- 旧用户目录中：contacts.dict.yaml 放在 `cn_dicts/`，并在 `rime_frost.dict.yaml` 的 import_tables 里加了 `- cn_dicts/contacts`；custom_phrase.txt 直接覆盖根目录同名文件。
- 备份/参考文件在 `D:\OneDrive\software\PC\rime\custom\`（installation-L.yaml / installation-home.yaml / weasel.custom.yaml 等），同步目录 `D:\OneDrive\software\PC\rime\sync`。

本次改动（均在 GitHub 仓库目录内）：
1. 新建 `installation.yaml`：installation_id: wtwo-L，sync_dir: "D:\\OneDrive\\software\\PC\\rime\\sync"。
2. 新建 `default.custom.yaml`：schema_list 只保留 `rime_frost_double_pinyin`。
3. 新建 `rime_frost_double_pinyin.custom.yaml`：patch `custom_phrase/user_dict: custom/custom_phrase`（tabledb 支持子目录路径，参考 en_dicts/cn_en_double_pinyin 先例；该 txt 无需 `#@/db_name` 头也能用，旧目录已验证）。
4. 修改 `rime_frost.dict.yaml`：import_tables 增加 `- custom/contacts`。**注意：这是被 git 跟踪的文件，upstream 更新后需重新加回。**
5. 修改 `.gitignore`：忽略 `/custom/`（内含身份证号、手机号等隐私）。

关键知识点：
- librime 的 `import_tables` **不支持递归**，被导入词库自身的 import_tables 不会展开（上游 issue #223 已确认），因此个人词库只能直接写进主词库的 import_tables，或把全部表扁平化到一个新 dict。
- `.dict.yaml` 不支持 `.dict.custom.yaml` 打补丁（DictSettings 直接读文件头，绕过 config 补丁机制）。
- 待用户确认：是否把小狼毫用户文件夹切到本仓库目录，以及迁移 `rime_frost.userdb`。

## 补充：全拼方案 + 拉取更新是否要重改

- `default.custom.yaml` 改为保留两个方案：双拼 `rime_frost_double_pinyin`（默认，排第一）+ 全拼 `rime_frost`（供他人临时切换）。
- 验证：全拼与双拼都 `dictionary: rime_frost`（即 rime_frost.dict.yaml），故 `- custom/contacts` 一行同时覆盖两个方案。
- 拉取更新安全性结论（git 状态核实）：`installation.yaml`、`*.custom.yaml`、`/custom/` 已被 .gitignore 忽略，普通 `git pull`/`git merge` 不会触碰这些被忽略文件 → 永久安全，无需重改。`rime_frost.dict.yaml`（contacts 行）与 `.gitignore` 是被跟踪的修改，是仅有的两处风险点；多数情况下 upstream 不动 import_tables，该行会随快进合并保留，建议每次 pull 后用 `grep -n "custom/contacts" rime_frost.dict.yaml` 复检。

## 新增改动：横排候选 + 符号表

- `weasel.custom.yaml`（已存在，`*.custom.yaml` 被忽略）：patch 增加 `"style/horizontal": true`，候选窗口改为横排（原只有 `"style/color_scheme": google`）。安全，pull 免疫；但小狼毫外观 UI 重新生成该文件可能覆盖手动改动。
- `symbols_v.yaml`（⚠️ 被 git 跟踪）：第 438 行 `/dui` 由 `[✓, ✔, ✅]` 改为 `[✓, ✔, ✅, ☑]`。符号表不支持 `.custom.yaml` 补丁，只能直接改文件，故成为第二个「pull 后可能要补回」的跟踪文件。复检：`grep -n "'/dui'" symbols_v.yaml`。
- 已同步更新 `custom/修改清单.md`：改动一览现 8 项，风险点明确为 #5 rime_frost.dict.yaml 与 #6 symbols_v.yaml 两处跟踪文件。

## 新增：custom/修改清单.md

- 在 `custom/修改清单.md` 整理全部个人改动的「目的+内容+拉取后是否要重改」，含复检命令与待办清单，防止遗忘。`custom/` 已被 /custom/ 忽略，该清单只留本地、不进版本库、对 git pull 免疫。

## 新增：开始菜单搜索自动英文 + 数字后星号

- 开始菜单搜索框聚焦时自动切英文：`weasel.custom.yaml`（已被忽略）patch 增加 `app_options/SearchHost.exe/ascii_mode: true`（Win11 `SearchHost.exe`）与 `app_options/SearchUI.exe/ascii_mode: true`（Win10 `SearchUI.exe`，兼容）。Win 版本进程名无法用 reg 读取（被安全策略禁用），故两个都写上。
- 扩展「数字后点号」到 `*`：新建 `lua/number_asterisk.lua`（未跟踪新文件）。逻辑：中文模式 + composing + input 全为数字时按 `*` → 提交 `数字+半角*`（commit_text + clear + return 1）；再次按 `*` → 非 composing，punctuator 在中文 full_shape 模式输出全角 `＊`（punctuation.yaml:27 首候选即 `＊`）。与内置数字后点号行为一致。
- 注册方式刻意用 `.custom.yaml` 补丁，避免改被跟踪的方案文件：`engine/processors/@before: punctuator: lua_processor@*number_asterisk`，加在 `rime_frost_double_pinyin.custom.yaml` 与 `rime_frost.custom.yaml`（全拼）中。两个方案共用，pull 安全。
- 复检结论：本轮未新增任何被跟踪风险点；被跟踪需 pull 后复检的仍只有 `rime_frost.dict.yaml`(#6) 与 `symbols_v.yaml`(#7) 两处。
- 待办（仍未做，方案不真正生效）：把小狼毫用户文件夹切到 `D:\Documents\GitHub\rime-frost` 并重新部署；迁移旧目录 `D:\Documents\Rime Config` 的 `rime_frost.userdb` 词频。

## 调试修复：数字后星号第一版失效

- 症状：重新部署后「数字后 *」不工作（`*` 始终出全角 ＊）。
- 根因：双拼/全拼 `rime_frost*.schema.yaml` 的 `speller.alphabet` 只含字母+反引号+斜杠，**不含数字**。数字键在空 composition 时直接上屏，不进入 `context.input`。第一版用 `context.input:match("^%d+$")` 判断，永远为假 → 处理器直接 return 2 放行。
- 验证手段：`%TEMP%/rime.weasel/rime.weasel.WTWO-L.wtwo.log.*` 可见 Lua 报错（来自 number_translator 的 `bad argument #5`，与本处理器无关）；build/ 编译产物确认 `lua_processor@*number_asterisk` 已正确进入部署链（注册没问题，问题在逻辑）。
- 修复（`lua/number_asterisk.lua` 重写）：用 `context.commit_notifier:connect(function(ctx) M.last_commit = ctx:get_commit_text() end)` 记录最近上屏文本；`*` 按下时若 `M.last_commit` 以数字结尾则 `engine:commit_text("*")`（半角），否则 return 2 交还标点表出 `＊`。同时保留情况 A（composing 且 input 为纯数字）以兼容未来把数字纳入 speller 的配置。commit_notifier API 参考仓库内 `lua/search.lua`。
- 仍建议用户重新部署后实测；若仍异常，可临时在处理器里加文件日志定位。

## 调试修复：数字后星号第二版仍不对（用户实测 * 后 . 直接中文句号）

- 用户真实期望（重新读原始需求「输入*号后，也实现这个功能，默认.再按才是」）：**数字后按 `*`，再按 `.` 应首次英文点、再按中文句号**——即把 `*` 当作「数字后」上下文延续符，让后续 `.` 也享受「数字后点号」。不是 `*` 自身半角/全角切换。
- 第一版（commit_notifier 判 last_commit 数字结尾）为何仍不对：`*` 在数字后已由标点表 `half_shape` 首候选（punctuation.yaml:61 `'*':['*',＊,...]`）输出半角 `*`，但 `*` 上屏清空 composition，后续 `.` 失去「数字后」上下文 → 标点表退回 full_shape 中文句号 `。`（punctuation.yaml:7 `'.':{commit:。}`）。
- 关键真相：**「数字后点号」靠 librime 内置 number_translator 把数字串作为候选留在 `context.composition`（未上屏），punctuator 检测 composition 以数字结尾才切半角**；直接按数字键（无 R 前缀，R 是 lua number_translator 的前缀）走的是内置 number_translator，composition 里有数字 segment。所以判断「数字后」必须看 `context.composition:back().text` 是否纯数字，而非 `context.input`（数字不走 speller，input 为空）也非仅 last_commit。
- 修复（`lua/number_asterisk.lua` 再重写）：
  1. `digit_context(context)` 用 `pcall` 取 `context.composition:back().text`，纯数字则判为数字后并拿到该数字文本；兜底用 commit_notifier 的 last_commit 数字结尾（数字已上屏情况）。
  2. `*` 且数字后：commit 半角 `*`（若数字在 composition 则连同数字文本一起 `engine:commit_text(digit_text.."*")`）并置 `pending_asterisk=true`；再按 `*` → 全角 `＊`。
  3. `.` 且 `pending_asterisk`：强制 `engine:commit_text(".")`（英文点），清 pending；再按 `.` 走标点表中文句号。
  4. 其他键清除 pending，避免误触。
  5. 带调试日志写入 `%TEMP%/number_asterisk.log`（init 时清空）。
- 待用户重新部署实测；若仍不对，让其发 `number_asterisk.log` 精确定位。本处理器用 `.custom.yaml` 注册，仍零新增 pull 风险点。

## 调试修复：数字后星号第三版——真正根因（key:repr() 大小写）

- 用户反馈上一版仍无效，且日志只有 `init` 一行。定位：build/ 编译产物确认 `lua_processor@*number_asterisk` 在部署链；后用户重部署后日志出现 `commit_notifier` 条目，证明 `func` 其实在跑——但**完全没有任何 `* pressed` 日志**。
- **真正根因**：`*` 键的 `key:repr()` 是**大写** `"Shift+8"`，而代码用**小写** `"shift+8"` 比对 → 不匹配 → `func` 把 `*` 当普通键走 `else` 分支 `return 2` 直接放行（不写日志），标点表接管：数字后 `*` 出半角 `*`、但 `pending_asterisk` 永远没被置位 → 紧随的 `.` 失去「数字后」上下文退回中文句号 `。`（与用户描述「*后.直接中文句号」完全吻合）。
- 旁证：本方案「数字」走 `lua_translator@*number_translator`（需 `R` 前缀，见 recognizer `number: "^R[0-9]+..."`）；裸数字 `12` 能进 composition（否则内置「数字后点号」不成立），故 `digit_context` 用 `context.composition:back().text` 判断可行。
- **修复**（`lua/number_asterisk.lua` 第三版）：`local lr = key:repr():lower()` 后再比对；`is_star` 宽松匹配 `(lr=="*" or (lr:find("shift") and lr:find("8")) or lr:find("asterisk"))`；`digit_context` 增加 `context.input` 数字结尾兜底；`func` 顶部对**每个按键**都写日志（`KEY repr=... composing=... pending=...`），便于万一还有偏差一次定位。
- 已同步更新 `custom/修改清单.md` 第 10 项，把根因从早期「context.input 无数字」假设修正为「`*` repr 大小写不匹配」。
- 用户需重新部署后实测：中文模式输入 `12` → 按 `*` 得 `12*` → 按 `.` 应得 `12*.`（英文点）；再按 `.` 中文句号。仍异常则发 `%TEMP%/number_asterisk.log`。

## 取消「数字后星号」任务 + 新增 Everything 搜索栏自动英文

- 用户认为星号功能调试太麻烦，**取消该任务**。已执行清理：`rm lua/number_asterisk.lua`；从 `rime_frost_double_pinyin.custom.yaml`、`rime_frost.custom.yaml` 移除 `lua_processor@*number_asterisk` 注册（后者改回 `patch: {}` 占位）；删除调试日志 `%TEMP%/number_asterisk.log`；`custom/修改清单.md` 重写，删 #10、加「已取消任务」段、#4 补充 Everything。
- **新增 Everything 自动英文**：`weasel.custom.yaml` 的 `app_options` 加 `"app_options/Everything.exe/ascii_mode": true`（进程名 `Everything.exe`），与开始菜单搜索（SearchHost.exe/SearchUI.exe）同款逻辑。重新部署即生效，pull 安全（*.custom.yaml 已被忽略）。

## 波浪号改中文 + 语言模型是否生效核实

- **波浪号**：用户要求中文状态下 `~`（`shift`+反引号键）输出中文波浪号 `～`。
  - 关键坑：**本仓库 `punctuation.yaml` 实际未被引用**，方案的 `punctuator` 通过 `__include: default:/punctuator/half_shape` 引用的是 `default.yaml` 内联标点（其中 `half_shape '~' : '~'` 在 default.yaml:141）。改 `punctuation.yaml` 无效。
  - 改法：在 `default.custom.yaml` 加 `patch: punctuator/half_shape/'~': [ '～', '~' ]`，经 include 同时覆盖双拼+全拼，且 `*.custom.yaml` 已忽略 → pull 安全。YAML 已校验通过。
- **语言模型核实结论：已生效（不需要改 rime_frost.custom.yaml）**。
  - 证据 1（运行日志 `%TEMP%/rime.weasel/*.INFO.*.log`）：`octagram.cc:29] use grammar: zh-moqi` 与 `gram_db.cc:14] loading gram db: D:\Documents\GitHub\rime-frost\zh-moqi.gram`，**无报错**，模型加载成功。
  - 证据 2：两方案 schema 已内联 grammar 配置（`language: zh-moqi` + `non_collocation_penalty: -4` + `grammar_penalty: -4` + `max_homophones: 4` + `max_homographs: 4`），与 readme 建议等价，故 readme 的「改 rime_frost.custom.yaml 加 grammar」冗余、无需做（当前 rime_frost.custom.yaml 仍是 `patch: {}`）。
  - 细节：readme 的 `collocation_penalty: -14 / collocation_max_length:5 / collocation_min_length:2 / contextual_suggestions:true / max_homographs:2` 在 schema 中**未配置**（schema 用 `grammar_penalty` 旧键 + `max_homographs:4`）。但 `zh-moqi` 是极小占位模型（schema 注释「很小几乎不起作用，但必须配置以让 non_collocation_penalty 生效」），collocation 类参数需更大 octagram 模型才有意义——真实词库联想需按 readme「选项 b」下载模型替换 zh-moqi。
  - 给用户结论：语言模型已加载并启用；若想获得真正的词组联想效果，按 readme 选项(b)替换 zh-moqi 模型即可，不必改 custom yaml。

## 波浪号补丁修正（第二版，关键坑）

- 第一版 `default.custom.yaml` 加 `punctuator/half_shape/'~': [ '～', '~' ]` **对实际输入无效**，用户实测未生效。
  - 根因：方案的 `punctuator/half_shape` 是 `__include: default:/punctuator/half_shape`，**此 include 不会带进 `default.custom.yaml` 的补丁**（已用 `build/` 编译产物核实：build/default.yaml 含 `"~":["～","~"]` 但 build/双拼schema 的 half_shape 仍是 `"~":"~"` 原始值）。
  - 第二错：用了**列表** `[ '～', '~' ]`，即使生效也是候选菜单而非直接上屏。
- **修正**：撤回 `default.custom.yaml` 的 half_shape 补丁；改为在**方案级** `.custom.yaml` 直接覆盖且用**单值** `'~': '～'`（按一下即中文波浪号）：
  - `rime_frost_double_pinyin.custom.yaml` 加 `punctuator/half_shape/'~': '～'`（与既有 `custom_phrase/user_dict` 同文件合并）；
  - `rime_frost.custom.yaml` 由 `patch: {}` 改为 `patch: punctuator/half_shape/'~': '～'`。
  - 三处文件均 `*.custom.yaml` 已忽略 → pull 安全。YAML 已校验通过。`custom/修改清单.md` 的 #2/#3/#5 与新增「2b 关键坑」段已同步修正。
